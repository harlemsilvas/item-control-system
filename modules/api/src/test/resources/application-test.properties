# Usado por testes de integra??o via Testcontainers
# A URI real deve ser definida via DynamicPropertySource nos testes ou via env var.
# Mantemos sem conex?o por padr?o para evitar timeouts em m?quinas sem Mongo rodando.
spring.data.mongodb.uri=mongodb://127.0.0.1:0/test
