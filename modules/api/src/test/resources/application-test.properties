# Usado por testes de integração via Testcontainers
spring.data.mongodb.uri=mongodb://localhost:27017/test

# Evita inicializações que dependem do Mongo no startup se necessário
# (os testes sobrescrevem a URI via DynamicPropertySource)
