package br.com.harlemsilvas.itemcontrol.core.domain.model;

/**
 * Escopo de um Template.
 */
public enum TemplateScope {
    /** Template geral, disponível a todos os usuários. */
    GLOBAL,
    /** Template privado, criado por um usuário e listado apenas para ele. */
    USER
}
